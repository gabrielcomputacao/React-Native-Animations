import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    justifyContent: "flex-end",
    alignContent: "center",
  },
  canvas: {
    width: 257,
    height: 249,
    zIndex: 1,
    position: "absolute",
  },
});
